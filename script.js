// get the element
const element = document.getElementById('page_visits')
var visitors = 0;
// always checking if the element is clicked, if so, do alert('hello')
element.addEventListener("click", () => {
  alert('hello');
});